# JS kafka AIngle Example

## Prerequisites

* `node`
* `@aingle/aiid-js`


## Running TESTS
* `npm install` - installs npm dependencies.
* `npm run start:producer` - starts producer.
* `npm run start:consumer` - starts consumer.
